(a) **Push autorizado.** Voy a correr `git push origin main` yo mismo en mi terminal (el harness te lo
bloquea, como ya sabemos) — no lo intentes tú.

(b) **Incluye el roadmap** en el commit B2B (`feat(db): B2B companies mechanism (inert, consent pending
legal review)` con los 4 archivos: migración + backup + audit doc + la línea del roadmap). Es normal que
el roadmap acumule diffs de varios commits — así se documentó también el fix del webhook y R4/R5. Crea el
commit.

Cuando termines, dime el conteo final de commits por delante de `origin/main` antes de que yo empuje.
